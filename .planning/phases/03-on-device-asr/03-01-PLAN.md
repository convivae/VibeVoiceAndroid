---
phase: 03-on-device-asr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloud_server/quantization/awq_quantize.py
  - cloud_server/quantization/export_tflite.py
  - cloud_server/quantization/validate_model.py
  - cloud_server/quantization/evaluate_wer.py
autonomous: false
requirements:
  - REQ-08
  - REQ-12
user_setup:
  - service: HuggingFace
    why: "Download VibeVoice-ASR base model and upload quantized model"
    env_vars:
      - name: HF_TOKEN
        source: "HuggingFace Settings -> Access Tokens"
  - service: RTX 4060 GPU Server
    why: "AWQ quantization requires 8GB+ VRAM"
    setup: "CUDA 12+, Python 3.10+, autoawq installed"
    note: "Must complete before Wave 2 tasks can run"

must_haves:
  truths:
    - "INT4 AWQ quantized model fits within size constraint"
    - "TFLite model successfully converts and loads"
    - "Quantized WER loss < 15% vs FP16 baseline"
  artifacts:
    - path: "cloud_server/quantization/awq_quantize.py"
      provides: "AWQ INT4 quantization of VibeVoice-ASR"
      min_lines: 100
    - path: "cloud_server/quantization/export_tflite.py"
      provides: "AWQ model to TFLite conversion pipeline"
      min_lines: 80
    - path: "cloud_server/quantization/validate_model.py"
      provides: "Model size and format validation"
      min_lines: 60
    - path: "cloud_server/quantization/vibevoice_asr_int4.tflite"
      provides: "Mobile-ready quantized model"
      exists: true
  key_links:
    - from: "awq_quantize.py"
      to: "export_tflite.py"
      via: "quantized model output path"
      pattern: "quantized_output.*export_tflite"
    - from: "export_tflite.py"
      to: "validate_model.py"
      via: "tflite file path"
      pattern: "tflite_output.*validate"
---

<objective>
Quantize VibeVoice-ASR (9B) using INT4 AWQ and export to TFLite format for mobile inference. **CRITICAL: Model is actually 9B, not 7B — size validation is a BLOCKING checkpoint.**
</objective>

Purpose: Server-side model preparation that produces a mobile-ready TFLite model < 500MB (target) or establishes realistic fallback targets.

Output:
- AWQ quantized model (INT4, w_bit=4, q_group_size=128 per D-11, D-12)
- TFLite model with SELECT_TF_OPS enabled
- Validation report: model size, basic inference test
- WER evaluation report vs FP16 baseline

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-on-device-asr/03-RESEARCH.md
@.planning/phases/03-on-device-asr/03-CONTEXT.md
@raw/SPEC.md §2.3.1 (INT4 AWQ quantization)
@raw/SPEC.md §2.3.2 (quantization reference code)

# Key constraints from RESEARCH.md:
# - VibeVoice-ASR is 9B parameters (not 7B)
# - Raw model 17.3GB BF16 → INT4 AWQ ~4-5GB
# - 500MB target is NOT achievable with full 9B model
# - AWQ → TFLite requires ONNX intermediate format
# - SELECT_TF_OPS required for custom ops

# Decision references:
# - D-01: TensorFlow Lite (NOT MNN/llama.cpp)
# - D-11: INT4 AWQ, w_bit=4, q_group_size=128
# - D-15: New OnDeviceAsrEngine class wrapping TFLite
</context>

<read_first>
cloud_server/quantization/awq_quantize.py (new file — see code example in 03-RESEARCH.md)
cloud_server/quantization/export_tflite.py (new file — see code example in 03-RESEARCH.md)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create AWQ Quantization Script</name>
  <files>cloud_server/quantization/awq_quantize.py</files>
  <action>
Create `cloud_server/quantization/awq_quantize.py` following the pattern in 03-RESEARCH.md §Code Examples:

1. **Import dependencies**: torch, transformers, AutoModelForCausalLM, AutoTokenizer, AutoAWQForCausalLM

2. **Configuration** (per locked decisions):
   - MODEL_PATH = "microsoft/VibeVoice-ASR"
   - QUANTIZED_OUTPUT = "./quantized_vibevoice_asr"
   - quant_config = {"zero_point": True, "q_group_size": 128, "w_bit": 4, "version": "GEMM"}

3. **Quantization steps**:
   - Load model with trust_remote_code=True, torch_dtype=torch.float16
   - Load tokenizer
   - Run AWQ calibration with 100 samples from LibrisSpeech
   - Save quantized model to QUANTIZED_OUTPUT

4. **Size reporting**: Print actual model size after quantization. If > 1GB, log WARNING that 500MB target may not be achievable.

**CRITICAL**: This script assumes RTX 4060 (8GB VRAM) available. If OOM, reduce batch size or use CPU offloading.

**D-11, D-12**: Use w_bit=4, q_group_size=128 exactly as specified.
</action>
  <verify>
<automated>
cd cloud_server/quantization && python -c "from awq import AutoAWQForCausalLM; print('AWQ import OK')"
# Run: python awq_quantize.py --dry-run (verify script syntax without full quantization)
</verify>
  <done>awq_quantize.py exists, imports work, dry-run passes</done>
</task>

<task type="auto">
  <name>Task 2: Create TFLite Export Script</name>
  <files>cloud_server/quantization/export_tflite.py</files>
  <action>
Create `cloud_server/quantization/export_tflite.py` following the AWQ → ONNX → TFLite path:

1. **AWQ to FP16 dequantization**:
   - Load AWQ quantized model
   - Dequantize to FP16 weights
   - Prepare example input for ONNX export

2. **ONNX export**:
   - Use torch.onnx.export with opset_version=14
   - Input: audio tensor (shape: [batch, 1, 16000])
   - Output: transcription text logits

3. **ONNX to TFLite conversion** (via onnx2tf):
   ```python
   convert(
       input_onnx_file_path="./vibevoice_asr.onnx",
       output_folder="./tflite_output",
       enable_select_tf_ops=True,  # REQUIRED per D-01 + custom ops
   )
   ```

4. **Model size report**: Print TFLite file size. If > 500MB, this is the blocking issue that must be resolved before Wave 2.

**CRITICAL PATH ISSUE**: If VibeVoice-ASR uses custom ops not supported by TFLite:
- Enable SELECT_TF_OPS (on)
- If still fails, document exact ops that fail and suggest fallback (smaller model variant)
- Cannot proceed to Wave 2 until TFLite model is verified

**D-01**: Use TensorFlow Lite, NOT MNN or other frameworks.
</action>
  <verify>
<automated>
# Verify onnx2tf is installed
pip show onnx2tf 2>/dev/null | grep -q "Version" && echo "onnx2tf OK"
# Verify conversion script syntax
cd cloud_server/quantization && python -c "import ast; ast.parse(open('export_tflite.py').read())" && echo "export_tflite.py syntax OK"
</verify>
  <done>export_tflite.py exists, syntax valid, dependencies installed</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Model Size Validation (BLOCKING)</name>
  <files>cloud_server/quantization/validate_model.py</files>
  <action>
## BLOCKING CHECKPOINT — Phase 3 cannot proceed if this fails

Run quantization and measure actual output size:

```bash
cd cloud_server/quantization
python awq_quantize.py
python export_tflite.py
ls -la tflite_output/*.tflite
```

**Acceptable sizes**:
- < 500MB: ✅ Proceed, goal achieved
- 500MB - 1GB: ⚠️ Proceed with update to REQ-08 target
- 1GB - 2GB: ⚠️ Proceed only if C-03 (memory < 2GB) is achievable via memory-mapped weights
- > 2GB: ❌ FAIL — Full 9B model too large, must use smaller variant or accept larger APK

**Decision options**:
- **option-a**: "Use smaller model variant" — Explore VibeVoice-ASR distil/whisper-tiny/base variants
- **option-b**: "Accept larger target" — Update REQ-08 to 2GB, proceed with full model
- **option-c**: "Use layer-wise loading" — Memory-map weights, only load needed layers
- **option-d**: "Abort Phase 3" — Fallback to cloud-only ASR

**Action required**: Document actual measured size, select option, update ROADMAP.md if needed.
</action>
  <how-to-verify>
1. Run: `python awq_quantize.py` (allow 30-60 min on RTX 4060)
2. Run: `python export_tflite.py` (allow 15-30 min)
3. Check: `du -h tflite_output/*.tflite`
4. Document: Actual size vs 500MB target
5. Decision: Select option-a/b/c/d based on results
</how-to-verify>
  <resume-signal>
Select: option-a (smaller variant), option-b (update target), option-c (memory-map), or option-d (abort)
</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Basic TFLite Inference Test</name>
  <files>cloud_server/quantization/validate_model.py</files>
  <action>
Create `validate_model.py` to verify the TFLite model can load and run basic inference:

1. **Load test**:
   ```python
   import tflite_runtime.interpreter as tflite
   interpreter = tflite.Interpreter(model_path="tflite_output/vibevoice_asr.tflite")
   interpreter.allocate_tensors()
   print(f"Input tensors: {len(interpreter.get_input_details())}")
   print(f"Output tensors: {len(interpreter.get_output_details())}")
   ```

2. **Basic inference test**:
   - Create dummy audio input (16kHz, 1 second)
   - Run inference
   - Verify output shape/type is valid

3. **Report**:
   - Model loads successfully: YES/NO
   - Inference completes: YES/NO
   - Output shape: [Nx, Ny]
   - Errors encountered: (if any)

**D-15**: This validates the OnDeviceAsrEngine will be able to use this model.
</action>
  <verify>
<automated>
# Install tflite-runtime for validation
pip install tflite-runtime
# Run validation
cd cloud_server/quantization && python validate_model.py
</verify>
  <done>TFLite model loads, basic inference passes, validation report generated</done>
</task>

<task type="auto">
  <name>Task 5: WER Evaluation (RE-12)</name>
  <files>cloud_server/quantization/evaluate_wer.py</files>
  <action>
Create `evaluate_wer.py` to evaluate ASR quality after quantization:

1. **Setup**:
   - Download LibrisSpeech test-clean subset (~270 samples)
   - Use whisper-metrics or evaluate-whisper library
   - Compare: FP16 baseline vs INT4 quantized model

2. **Evaluation**:
   ```python
   # Run FP16 baseline
   python evaluate_wer.py --model-path microsoft/VibeVoice-ASR --dtype fp16 --dataset librispeech
   
   # Run INT4 quantized
   python evaluate_wer.py --model-path ./quantized_vibevoice_asr --dtype int4 --dataset librispeech
   
   # Calculate WER difference
   wer_diff = wer_int4 - wer_fp16
   print(f"WER diff: {wer_diff:.2%} (target: < 15%)")
   ```

3. **Success criteria**: WER loss < 15% (REQ-12)

**NOTE**: WER evaluation requires actual model output, which depends on TFLite conversion working. If conversion failed in Task 3, skip this task.
</action>
  <verify>
<automated>
# Check LibrisSpeech dataset available
python -c "import datasets; ds = datasets.load_dataset('librispeech_asr', 'test', split='clean')" 2>/dev/null && echo "LibrisSpeech OK" || echo "Need to download"
# Run WER evaluation (if model available)
cd cloud_server/quantization && python evaluate_wer.py --quick 2>/dev/null || echo "Skipped (model not ready)"
</verify>
  <done>WER evaluation report exists, WER loss documented, REQ-12 status known</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External network → Server | Downloading model from HuggingFace, uploading to model server |
| Server local storage | Quantized model stored locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | AWQ quantization | mitigate | Verify quantized model integrity with SHA256 before upload |
| T-03-02 | Information Disclosure | Quantized model weights | accept | Model weights are public knowledge, no PII involved |
| T-03-03 | Denial of Service | GPU OOM during quantization | mitigate | Add OOM detection and graceful fallback to smaller batch size |
</threat_model>

<verification>
- [ ] awq_quantize.py runs without syntax errors
- [ ] export_tflite.py runs without syntax errors
- [ ] Model size measured and documented (BLOCKING decision)
- [ ] TFLite model loads successfully (if conversion succeeded)
- [ ] WER evaluation completed (if model ready)
</verification>

<success_criteria>
- Quantized TFLite model exists and loads correctly
- Model size is documented (whether < 500MB or not)
- If > 500MB, decision made on how to proceed
- WER evaluation completed with REQ-12 status
- All artifacts committed to repository
</success_criteria>

<output>
After completion, create `.planning/phases/03-on-device-asr/03-01-SUMMARY.md`
</output>